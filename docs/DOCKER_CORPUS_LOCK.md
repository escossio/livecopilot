# DOCKER CORPUS LOCK

## Locked corpus
O domínio `docker_core` inclui exatamente os seguintes documentos oficiais:

- Docker docs home: `https://docs.docker.com/`
- Docker CLI reference
  - `docker build`
  - `docker run`
  - `docker ps`
  - `docker logs`
  - `docker exec`
  - `docker volume`
  - `docker network`
- Dockerfile reference
  - syntax and instructions
- Core concept pages
  - containers
  - images
  - layers
  - registries

## Lock rule
- Nenhum documento fora desta lista entra no corpus sem atualização explícita deste lock.
- O escopo permanece estritamente core.
