# HANDOFF_LIVECOPILOT_DOCKER_LEXICAL_BASELINE_20260319T020109Z

## Perguntas testadas
- What is a container? -> `RESPONDIVEL` via `docker-container-0001`
- What is a Docker image? -> `RESPONDIVEL` via `docker-image-layers-0002`
- What is a Dockerfile? -> `RESPONDIVEL` via `docker-dockerfile-0003`
- What does docker build do? -> `RESPONDIVEL` via `docker-cli-build-0002`
- What does docker run do? -> `RESPONDIVEL` via `docker-cli-run-0001`
- What does docker ps do? -> `RESPONDIVEL` via `docker-cli-ps-0003`
- What does docker logs do? -> `RESPONDIVEL` via `docker-cli-logs-0004`
- What does docker exec do? -> `RESPONDIVEL` via `docker-cli-exec-0005`
- What are Docker volumes? -> `RESPONDIVEL` via `docker-volumes-0001`
- What are Docker networks? -> `RESPONDIVEL` via `docker-networks-0001`
- What is a Docker registry? -> `RESPONDIVEL` via `docker-registry-0002`
- What are image layers? -> `RESPONDIVEL` via `docker-image-layers-0002`

## Cobertura obtida
- RESPONDIVEL: 12/12
- PARCIALMENTE_RESPONDIVEL: 0/12
- NAO_RESPONDIVEL: 0/12

## Lacunas encontradas
- Nenhuma lacuna bloqueante na bateria mínima.

## Pronto para semantic_embeddings
- Sim, a cobertura lexical mínima está satisfatória para avançar depois que a frente autorizar a próxima etapa.

## Observação
- Não houve expansão de corpus nem geração de embeddings nesta rodada.
